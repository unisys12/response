// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/responserms/response/internal/ent/metadata"
	"github.com/responserms/response/internal/ent/predicate"
	"github.com/responserms/response/internal/ent/vehicle"
	"github.com/responserms/response/internal/ent/vehiclecolor"
)

// VehicleColorQuery is the builder for querying VehicleColor entities.
type VehicleColorQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.VehicleColor
	// eager-loading edges.
	withMetadata      *MetadataQuery
	withVehiclesMajor *VehicleQuery
	withVehiclesMinor *VehicleQuery
	withFKs           bool
	modifiers         []func(s *sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VehicleColorQuery builder.
func (vcq *VehicleColorQuery) Where(ps ...predicate.VehicleColor) *VehicleColorQuery {
	vcq.predicates = append(vcq.predicates, ps...)
	return vcq
}

// Limit adds a limit step to the query.
func (vcq *VehicleColorQuery) Limit(limit int) *VehicleColorQuery {
	vcq.limit = &limit
	return vcq
}

// Offset adds an offset step to the query.
func (vcq *VehicleColorQuery) Offset(offset int) *VehicleColorQuery {
	vcq.offset = &offset
	return vcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vcq *VehicleColorQuery) Unique(unique bool) *VehicleColorQuery {
	vcq.unique = &unique
	return vcq
}

// Order adds an order step to the query.
func (vcq *VehicleColorQuery) Order(o ...OrderFunc) *VehicleColorQuery {
	vcq.order = append(vcq.order, o...)
	return vcq
}

// QueryMetadata chains the current query on the "metadata" edge.
func (vcq *VehicleColorQuery) QueryMetadata() *MetadataQuery {
	query := &MetadataQuery{config: vcq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(vehiclecolor.Table, vehiclecolor.FieldID, selector),
			sqlgraph.To(metadata.Table, metadata.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, vehiclecolor.MetadataTable, vehiclecolor.MetadataColumn),
		)
		fromU = sqlgraph.SetNeighbors(vcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVehiclesMajor chains the current query on the "vehicles_major" edge.
func (vcq *VehicleColorQuery) QueryVehiclesMajor() *VehicleQuery {
	query := &VehicleQuery{config: vcq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(vehiclecolor.Table, vehiclecolor.FieldID, selector),
			sqlgraph.To(vehicle.Table, vehicle.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, vehiclecolor.VehiclesMajorTable, vehiclecolor.VehiclesMajorColumn),
		)
		fromU = sqlgraph.SetNeighbors(vcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVehiclesMinor chains the current query on the "vehicles_minor" edge.
func (vcq *VehicleColorQuery) QueryVehiclesMinor() *VehicleQuery {
	query := &VehicleQuery{config: vcq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(vehiclecolor.Table, vehiclecolor.FieldID, selector),
			sqlgraph.To(vehicle.Table, vehicle.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, vehiclecolor.VehiclesMinorTable, vehiclecolor.VehiclesMinorColumn),
		)
		fromU = sqlgraph.SetNeighbors(vcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VehicleColor entity from the query.
// Returns a *NotFoundError when no VehicleColor was found.
func (vcq *VehicleColorQuery) First(ctx context.Context) (*VehicleColor, error) {
	nodes, err := vcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{vehiclecolor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vcq *VehicleColorQuery) FirstX(ctx context.Context) *VehicleColor {
	node, err := vcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VehicleColor ID from the query.
// Returns a *NotFoundError when no VehicleColor ID was found.
func (vcq *VehicleColorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{vehiclecolor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vcq *VehicleColorQuery) FirstIDX(ctx context.Context) int {
	id, err := vcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VehicleColor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one VehicleColor entity is not found.
// Returns a *NotFoundError when no VehicleColor entities are found.
func (vcq *VehicleColorQuery) Only(ctx context.Context) (*VehicleColor, error) {
	nodes, err := vcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{vehiclecolor.Label}
	default:
		return nil, &NotSingularError{vehiclecolor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vcq *VehicleColorQuery) OnlyX(ctx context.Context) *VehicleColor {
	node, err := vcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VehicleColor ID in the query.
// Returns a *NotSingularError when exactly one VehicleColor ID is not found.
// Returns a *NotFoundError when no entities are found.
func (vcq *VehicleColorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = &NotSingularError{vehiclecolor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vcq *VehicleColorQuery) OnlyIDX(ctx context.Context) int {
	id, err := vcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VehicleColors.
func (vcq *VehicleColorQuery) All(ctx context.Context) ([]*VehicleColor, error) {
	if err := vcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return vcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (vcq *VehicleColorQuery) AllX(ctx context.Context) []*VehicleColor {
	nodes, err := vcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VehicleColor IDs.
func (vcq *VehicleColorQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := vcq.Select(vehiclecolor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vcq *VehicleColorQuery) IDsX(ctx context.Context) []int {
	ids, err := vcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vcq *VehicleColorQuery) Count(ctx context.Context) (int, error) {
	if err := vcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return vcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (vcq *VehicleColorQuery) CountX(ctx context.Context) int {
	count, err := vcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vcq *VehicleColorQuery) Exist(ctx context.Context) (bool, error) {
	if err := vcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return vcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (vcq *VehicleColorQuery) ExistX(ctx context.Context) bool {
	exist, err := vcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VehicleColorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vcq *VehicleColorQuery) Clone() *VehicleColorQuery {
	if vcq == nil {
		return nil
	}
	return &VehicleColorQuery{
		config:            vcq.config,
		limit:             vcq.limit,
		offset:            vcq.offset,
		order:             append([]OrderFunc{}, vcq.order...),
		predicates:        append([]predicate.VehicleColor{}, vcq.predicates...),
		withMetadata:      vcq.withMetadata.Clone(),
		withVehiclesMajor: vcq.withVehiclesMajor.Clone(),
		withVehiclesMinor: vcq.withVehiclesMinor.Clone(),
		// clone intermediate query.
		sql:  vcq.sql.Clone(),
		path: vcq.path,
	}
}

// WithMetadata tells the query-builder to eager-load the nodes that are connected to
// the "metadata" edge. The optional arguments are used to configure the query builder of the edge.
func (vcq *VehicleColorQuery) WithMetadata(opts ...func(*MetadataQuery)) *VehicleColorQuery {
	query := &MetadataQuery{config: vcq.config}
	for _, opt := range opts {
		opt(query)
	}
	vcq.withMetadata = query
	return vcq
}

// WithVehiclesMajor tells the query-builder to eager-load the nodes that are connected to
// the "vehicles_major" edge. The optional arguments are used to configure the query builder of the edge.
func (vcq *VehicleColorQuery) WithVehiclesMajor(opts ...func(*VehicleQuery)) *VehicleColorQuery {
	query := &VehicleQuery{config: vcq.config}
	for _, opt := range opts {
		opt(query)
	}
	vcq.withVehiclesMajor = query
	return vcq
}

// WithVehiclesMinor tells the query-builder to eager-load the nodes that are connected to
// the "vehicles_minor" edge. The optional arguments are used to configure the query builder of the edge.
func (vcq *VehicleColorQuery) WithVehiclesMinor(opts ...func(*VehicleQuery)) *VehicleColorQuery {
	query := &VehicleQuery{config: vcq.config}
	for _, opt := range opts {
		opt(query)
	}
	vcq.withVehiclesMinor = query
	return vcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VehicleColor.Query().
//		GroupBy(vehiclecolor.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (vcq *VehicleColorQuery) GroupBy(field string, fields ...string) *VehicleColorGroupBy {
	group := &VehicleColorGroupBy{config: vcq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := vcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return vcq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.VehicleColor.Query().
//		Select(vehiclecolor.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (vcq *VehicleColorQuery) Select(fields ...string) *VehicleColorSelect {
	vcq.fields = append(vcq.fields, fields...)
	return &VehicleColorSelect{VehicleColorQuery: vcq}
}

func (vcq *VehicleColorQuery) prepareQuery(ctx context.Context) error {
	for _, f := range vcq.fields {
		if !vehiclecolor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vcq.path != nil {
		prev, err := vcq.path(ctx)
		if err != nil {
			return err
		}
		vcq.sql = prev
	}
	return nil
}

func (vcq *VehicleColorQuery) sqlAll(ctx context.Context) ([]*VehicleColor, error) {
	var (
		nodes       = []*VehicleColor{}
		withFKs     = vcq.withFKs
		_spec       = vcq.querySpec()
		loadedTypes = [3]bool{
			vcq.withMetadata != nil,
			vcq.withVehiclesMajor != nil,
			vcq.withVehiclesMinor != nil,
		}
	)
	if vcq.withMetadata != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, vehiclecolor.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &VehicleColor{config: vcq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(vcq.modifiers) > 0 {
		_spec.Modifiers = vcq.modifiers
	}
	if err := sqlgraph.QueryNodes(ctx, vcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := vcq.withMetadata; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*VehicleColor)
		for i := range nodes {
			if nodes[i].vehicle_color_metadata == nil {
				continue
			}
			fk := *nodes[i].vehicle_color_metadata
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(metadata.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "vehicle_color_metadata" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Metadata = n
			}
		}
	}

	if query := vcq.withVehiclesMajor; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*VehicleColor)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.VehiclesMajor = []*Vehicle{}
		}
		query.withFKs = true
		query.Where(predicate.Vehicle(func(s *sql.Selector) {
			s.Where(sql.InValues(vehiclecolor.VehiclesMajorColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.vehicle_color_vehicles_major
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "vehicle_color_vehicles_major" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "vehicle_color_vehicles_major" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.VehiclesMajor = append(node.Edges.VehiclesMajor, n)
		}
	}

	if query := vcq.withVehiclesMinor; query != nil {
		fks := make([]driver.Value, 0, len(nodes))
		nodeids := make(map[int]*VehicleColor)
		for i := range nodes {
			fks = append(fks, nodes[i].ID)
			nodeids[nodes[i].ID] = nodes[i]
			nodes[i].Edges.VehiclesMinor = []*Vehicle{}
		}
		query.withFKs = true
		query.Where(predicate.Vehicle(func(s *sql.Selector) {
			s.Where(sql.InValues(vehiclecolor.VehiclesMinorColumn, fks...))
		}))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			fk := n.vehicle_color_vehicles_minor
			if fk == nil {
				return nil, fmt.Errorf(`foreign-key "vehicle_color_vehicles_minor" is nil for node %v`, n.ID)
			}
			node, ok := nodeids[*fk]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "vehicle_color_vehicles_minor" returned %v for node %v`, *fk, n.ID)
			}
			node.Edges.VehiclesMinor = append(node.Edges.VehiclesMinor, n)
		}
	}

	return nodes, nil
}

func (vcq *VehicleColorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vcq.querySpec()
	if len(vcq.modifiers) > 0 {
		_spec.Modifiers = vcq.modifiers
	}
	return sqlgraph.CountNodes(ctx, vcq.driver, _spec)
}

func (vcq *VehicleColorQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := vcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (vcq *VehicleColorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   vehiclecolor.Table,
			Columns: vehiclecolor.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: vehiclecolor.FieldID,
			},
		},
		From:   vcq.sql,
		Unique: true,
	}
	if unique := vcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := vcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, vehiclecolor.FieldID)
		for i := range fields {
			if fields[i] != vehiclecolor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vcq *VehicleColorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vcq.driver.Dialect())
	t1 := builder.Table(vehiclecolor.Table)
	columns := vcq.fields
	if len(columns) == 0 {
		columns = vehiclecolor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vcq.sql != nil {
		selector = vcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, m := range vcq.modifiers {
		m(selector)
	}
	for _, p := range vcq.predicates {
		p(selector)
	}
	for _, p := range vcq.order {
		p(selector)
	}
	if offset := vcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (vcq *VehicleColorQuery) ForUpdate(opts ...sql.LockOption) *VehicleColorQuery {
	if vcq.driver.Dialect() == dialect.Postgres {
		vcq.Unique(false)
	}
	vcq.modifiers = append(vcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return vcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (vcq *VehicleColorQuery) ForShare(opts ...sql.LockOption) *VehicleColorQuery {
	if vcq.driver.Dialect() == dialect.Postgres {
		vcq.Unique(false)
	}
	vcq.modifiers = append(vcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return vcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (vcq *VehicleColorQuery) Modify(modifiers ...func(s *sql.Selector)) *VehicleColorSelect {
	vcq.modifiers = append(vcq.modifiers, modifiers...)
	return vcq.Select()
}

// VehicleColorGroupBy is the group-by builder for VehicleColor entities.
type VehicleColorGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vcgb *VehicleColorGroupBy) Aggregate(fns ...AggregateFunc) *VehicleColorGroupBy {
	vcgb.fns = append(vcgb.fns, fns...)
	return vcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (vcgb *VehicleColorGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := vcgb.path(ctx)
	if err != nil {
		return err
	}
	vcgb.sql = query
	return vcgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := vcgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(vcgb.fields) > 1 {
		return nil, errors.New("ent: VehicleColorGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := vcgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) StringsX(ctx context.Context) []string {
	v, err := vcgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = vcgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) StringX(ctx context.Context) string {
	v, err := vcgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(vcgb.fields) > 1 {
		return nil, errors.New("ent: VehicleColorGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := vcgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) IntsX(ctx context.Context) []int {
	v, err := vcgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = vcgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) IntX(ctx context.Context) int {
	v, err := vcgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(vcgb.fields) > 1 {
		return nil, errors.New("ent: VehicleColorGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := vcgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := vcgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = vcgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) Float64X(ctx context.Context) float64 {
	v, err := vcgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(vcgb.fields) > 1 {
		return nil, errors.New("ent: VehicleColorGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := vcgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := vcgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (vcgb *VehicleColorGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = vcgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (vcgb *VehicleColorGroupBy) BoolX(ctx context.Context) bool {
	v, err := vcgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (vcgb *VehicleColorGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range vcgb.fields {
		if !vehiclecolor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := vcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (vcgb *VehicleColorGroupBy) sqlQuery() *sql.Selector {
	selector := vcgb.sql.Select()
	aggregation := make([]string, 0, len(vcgb.fns))
	for _, fn := range vcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(vcgb.fields)+len(vcgb.fns))
		for _, f := range vcgb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(vcgb.fields...)...)
}

// VehicleColorSelect is the builder for selecting fields of VehicleColor entities.
type VehicleColorSelect struct {
	*VehicleColorQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (vcs *VehicleColorSelect) Scan(ctx context.Context, v interface{}) error {
	if err := vcs.prepareQuery(ctx); err != nil {
		return err
	}
	vcs.sql = vcs.VehicleColorQuery.sqlQuery(ctx)
	return vcs.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (vcs *VehicleColorSelect) ScanX(ctx context.Context, v interface{}) {
	if err := vcs.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Strings(ctx context.Context) ([]string, error) {
	if len(vcs.fields) > 1 {
		return nil, errors.New("ent: VehicleColorSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := vcs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (vcs *VehicleColorSelect) StringsX(ctx context.Context) []string {
	v, err := vcs.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = vcs.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (vcs *VehicleColorSelect) StringX(ctx context.Context) string {
	v, err := vcs.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Ints(ctx context.Context) ([]int, error) {
	if len(vcs.fields) > 1 {
		return nil, errors.New("ent: VehicleColorSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := vcs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (vcs *VehicleColorSelect) IntsX(ctx context.Context) []int {
	v, err := vcs.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = vcs.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (vcs *VehicleColorSelect) IntX(ctx context.Context) int {
	v, err := vcs.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(vcs.fields) > 1 {
		return nil, errors.New("ent: VehicleColorSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := vcs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (vcs *VehicleColorSelect) Float64sX(ctx context.Context) []float64 {
	v, err := vcs.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = vcs.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (vcs *VehicleColorSelect) Float64X(ctx context.Context) float64 {
	v, err := vcs.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(vcs.fields) > 1 {
		return nil, errors.New("ent: VehicleColorSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := vcs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (vcs *VehicleColorSelect) BoolsX(ctx context.Context) []bool {
	v, err := vcs.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (vcs *VehicleColorSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = vcs.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{vehiclecolor.Label}
	default:
		err = fmt.Errorf("ent: VehicleColorSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (vcs *VehicleColorSelect) BoolX(ctx context.Context) bool {
	v, err := vcs.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (vcs *VehicleColorSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := vcs.sql.Query()
	if err := vcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (vcs *VehicleColorSelect) Modify(modifiers ...func(s *sql.Selector)) *VehicleColorSelect {
	vcs.modifiers = append(vcs.modifiers, modifiers...)
	return vcs
}
