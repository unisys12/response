// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/responserms/response/internal/ent/oauthconnection"
	"github.com/responserms/response/internal/ent/predicate"
	"github.com/responserms/response/internal/ent/user"
)

// OAuthConnectionQuery is the builder for querying OAuthConnection entities.
type OAuthConnectionQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.OAuthConnection
	// eager-loading edges.
	withUser  *UserQuery
	withFKs   bool
	modifiers []func(s *sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OAuthConnectionQuery builder.
func (ocq *OAuthConnectionQuery) Where(ps ...predicate.OAuthConnection) *OAuthConnectionQuery {
	ocq.predicates = append(ocq.predicates, ps...)
	return ocq
}

// Limit adds a limit step to the query.
func (ocq *OAuthConnectionQuery) Limit(limit int) *OAuthConnectionQuery {
	ocq.limit = &limit
	return ocq
}

// Offset adds an offset step to the query.
func (ocq *OAuthConnectionQuery) Offset(offset int) *OAuthConnectionQuery {
	ocq.offset = &offset
	return ocq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ocq *OAuthConnectionQuery) Unique(unique bool) *OAuthConnectionQuery {
	ocq.unique = &unique
	return ocq
}

// Order adds an order step to the query.
func (ocq *OAuthConnectionQuery) Order(o ...OrderFunc) *OAuthConnectionQuery {
	ocq.order = append(ocq.order, o...)
	return ocq
}

// QueryUser chains the current query on the "user" edge.
func (ocq *OAuthConnectionQuery) QueryUser() *UserQuery {
	query := &UserQuery{config: ocq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ocq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ocq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(oauthconnection.Table, oauthconnection.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, oauthconnection.UserTable, oauthconnection.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(ocq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OAuthConnection entity from the query.
// Returns a *NotFoundError when no OAuthConnection was found.
func (ocq *OAuthConnectionQuery) First(ctx context.Context) (*OAuthConnection, error) {
	nodes, err := ocq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{oauthconnection.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) FirstX(ctx context.Context) *OAuthConnection {
	node, err := ocq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OAuthConnection ID from the query.
// Returns a *NotFoundError when no OAuthConnection ID was found.
func (ocq *OAuthConnectionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ocq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{oauthconnection.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) FirstIDX(ctx context.Context) int {
	id, err := ocq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OAuthConnection entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when exactly one OAuthConnection entity is not found.
// Returns a *NotFoundError when no OAuthConnection entities are found.
func (ocq *OAuthConnectionQuery) Only(ctx context.Context) (*OAuthConnection, error) {
	nodes, err := ocq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{oauthconnection.Label}
	default:
		return nil, &NotSingularError{oauthconnection.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) OnlyX(ctx context.Context) *OAuthConnection {
	node, err := ocq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OAuthConnection ID in the query.
// Returns a *NotSingularError when exactly one OAuthConnection ID is not found.
// Returns a *NotFoundError when no entities are found.
func (ocq *OAuthConnectionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ocq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = &NotSingularError{oauthconnection.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) OnlyIDX(ctx context.Context) int {
	id, err := ocq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OAuthConnections.
func (ocq *OAuthConnectionQuery) All(ctx context.Context) ([]*OAuthConnection, error) {
	if err := ocq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return ocq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) AllX(ctx context.Context) []*OAuthConnection {
	nodes, err := ocq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OAuthConnection IDs.
func (ocq *OAuthConnectionQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := ocq.Select(oauthconnection.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) IDsX(ctx context.Context) []int {
	ids, err := ocq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ocq *OAuthConnectionQuery) Count(ctx context.Context) (int, error) {
	if err := ocq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return ocq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) CountX(ctx context.Context) int {
	count, err := ocq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ocq *OAuthConnectionQuery) Exist(ctx context.Context) (bool, error) {
	if err := ocq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return ocq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (ocq *OAuthConnectionQuery) ExistX(ctx context.Context) bool {
	exist, err := ocq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OAuthConnectionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ocq *OAuthConnectionQuery) Clone() *OAuthConnectionQuery {
	if ocq == nil {
		return nil
	}
	return &OAuthConnectionQuery{
		config:     ocq.config,
		limit:      ocq.limit,
		offset:     ocq.offset,
		order:      append([]OrderFunc{}, ocq.order...),
		predicates: append([]predicate.OAuthConnection{}, ocq.predicates...),
		withUser:   ocq.withUser.Clone(),
		// clone intermediate query.
		sql:  ocq.sql.Clone(),
		path: ocq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (ocq *OAuthConnectionQuery) WithUser(opts ...func(*UserQuery)) *OAuthConnectionQuery {
	query := &UserQuery{config: ocq.config}
	for _, opt := range opts {
		opt(query)
	}
	ocq.withUser = query
	return ocq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OAuthConnection.Query().
//		GroupBy(oauthconnection.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (ocq *OAuthConnectionQuery) GroupBy(field string, fields ...string) *OAuthConnectionGroupBy {
	group := &OAuthConnectionGroupBy{config: ocq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := ocq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return ocq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.OAuthConnection.Query().
//		Select(oauthconnection.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (ocq *OAuthConnectionQuery) Select(fields ...string) *OAuthConnectionSelect {
	ocq.fields = append(ocq.fields, fields...)
	return &OAuthConnectionSelect{OAuthConnectionQuery: ocq}
}

func (ocq *OAuthConnectionQuery) prepareQuery(ctx context.Context) error {
	for _, f := range ocq.fields {
		if !oauthconnection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ocq.path != nil {
		prev, err := ocq.path(ctx)
		if err != nil {
			return err
		}
		ocq.sql = prev
	}
	return nil
}

func (ocq *OAuthConnectionQuery) sqlAll(ctx context.Context) ([]*OAuthConnection, error) {
	var (
		nodes       = []*OAuthConnection{}
		withFKs     = ocq.withFKs
		_spec       = ocq.querySpec()
		loadedTypes = [1]bool{
			ocq.withUser != nil,
		}
	)
	if ocq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, oauthconnection.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &OAuthConnection{config: ocq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ocq.modifiers) > 0 {
		_spec.Modifiers = ocq.modifiers
	}
	if err := sqlgraph.QueryNodes(ctx, ocq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := ocq.withUser; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*OAuthConnection)
		for i := range nodes {
			if nodes[i].user_oauth_connections == nil {
				continue
			}
			fk := *nodes[i].user_oauth_connections
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(user.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "user_oauth_connections" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.User = n
			}
		}
	}

	return nodes, nil
}

func (ocq *OAuthConnectionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ocq.querySpec()
	if len(ocq.modifiers) > 0 {
		_spec.Modifiers = ocq.modifiers
	}
	return sqlgraph.CountNodes(ctx, ocq.driver, _spec)
}

func (ocq *OAuthConnectionQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := ocq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (ocq *OAuthConnectionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   oauthconnection.Table,
			Columns: oauthconnection.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: oauthconnection.FieldID,
			},
		},
		From:   ocq.sql,
		Unique: true,
	}
	if unique := ocq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := ocq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, oauthconnection.FieldID)
		for i := range fields {
			if fields[i] != oauthconnection.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ocq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ocq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ocq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ocq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ocq *OAuthConnectionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ocq.driver.Dialect())
	t1 := builder.Table(oauthconnection.Table)
	columns := ocq.fields
	if len(columns) == 0 {
		columns = oauthconnection.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ocq.sql != nil {
		selector = ocq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	for _, m := range ocq.modifiers {
		m(selector)
	}
	for _, p := range ocq.predicates {
		p(selector)
	}
	for _, p := range ocq.order {
		p(selector)
	}
	if offset := ocq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ocq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ocq *OAuthConnectionQuery) ForUpdate(opts ...sql.LockOption) *OAuthConnectionQuery {
	if ocq.driver.Dialect() == dialect.Postgres {
		ocq.Unique(false)
	}
	ocq.modifiers = append(ocq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ocq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ocq *OAuthConnectionQuery) ForShare(opts ...sql.LockOption) *OAuthConnectionQuery {
	if ocq.driver.Dialect() == dialect.Postgres {
		ocq.Unique(false)
	}
	ocq.modifiers = append(ocq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ocq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ocq *OAuthConnectionQuery) Modify(modifiers ...func(s *sql.Selector)) *OAuthConnectionSelect {
	ocq.modifiers = append(ocq.modifiers, modifiers...)
	return ocq.Select()
}

// OAuthConnectionGroupBy is the group-by builder for OAuthConnection entities.
type OAuthConnectionGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ocgb *OAuthConnectionGroupBy) Aggregate(fns ...AggregateFunc) *OAuthConnectionGroupBy {
	ocgb.fns = append(ocgb.fns, fns...)
	return ocgb
}

// Scan applies the group-by query and scans the result into the given value.
func (ocgb *OAuthConnectionGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := ocgb.path(ctx)
	if err != nil {
		return err
	}
	ocgb.sql = query
	return ocgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := ocgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(ocgb.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := ocgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) StringsX(ctx context.Context) []string {
	v, err := ocgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = ocgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) StringX(ctx context.Context) string {
	v, err := ocgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(ocgb.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := ocgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) IntsX(ctx context.Context) []int {
	v, err := ocgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = ocgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) IntX(ctx context.Context) int {
	v, err := ocgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(ocgb.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := ocgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := ocgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = ocgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) Float64X(ctx context.Context) float64 {
	v, err := ocgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(ocgb.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := ocgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := ocgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (ocgb *OAuthConnectionGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = ocgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (ocgb *OAuthConnectionGroupBy) BoolX(ctx context.Context) bool {
	v, err := ocgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (ocgb *OAuthConnectionGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range ocgb.fields {
		if !oauthconnection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := ocgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ocgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (ocgb *OAuthConnectionGroupBy) sqlQuery() *sql.Selector {
	selector := ocgb.sql.Select()
	aggregation := make([]string, 0, len(ocgb.fns))
	for _, fn := range ocgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(ocgb.fields)+len(ocgb.fns))
		for _, f := range ocgb.fields {
			columns = append(columns, selector.C(f))
		}
		for _, c := range aggregation {
			columns = append(columns, c)
		}
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(ocgb.fields...)...)
}

// OAuthConnectionSelect is the builder for selecting fields of OAuthConnection entities.
type OAuthConnectionSelect struct {
	*OAuthConnectionQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ocs *OAuthConnectionSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ocs.prepareQuery(ctx); err != nil {
		return err
	}
	ocs.sql = ocs.OAuthConnectionQuery.sqlQuery(ctx)
	return ocs.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) ScanX(ctx context.Context, v interface{}) {
	if err := ocs.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Strings(ctx context.Context) ([]string, error) {
	if len(ocs.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := ocs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) StringsX(ctx context.Context) []string {
	v, err := ocs.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = ocs.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) StringX(ctx context.Context) string {
	v, err := ocs.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Ints(ctx context.Context) ([]int, error) {
	if len(ocs.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := ocs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) IntsX(ctx context.Context) []int {
	v, err := ocs.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = ocs.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) IntX(ctx context.Context) int {
	v, err := ocs.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(ocs.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := ocs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) Float64sX(ctx context.Context) []float64 {
	v, err := ocs.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = ocs.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) Float64X(ctx context.Context) float64 {
	v, err := ocs.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(ocs.fields) > 1 {
		return nil, errors.New("ent: OAuthConnectionSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := ocs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) BoolsX(ctx context.Context) []bool {
	v, err := ocs.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (ocs *OAuthConnectionSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = ocs.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{oauthconnection.Label}
	default:
		err = fmt.Errorf("ent: OAuthConnectionSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (ocs *OAuthConnectionSelect) BoolX(ctx context.Context) bool {
	v, err := ocs.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (ocs *OAuthConnectionSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ocs.sql.Query()
	if err := ocs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ocs *OAuthConnectionSelect) Modify(modifiers ...func(s *sql.Selector)) *OAuthConnectionSelect {
	ocs.modifiers = append(ocs.modifiers, modifiers...)
	return ocs
}
