// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/responserms/response/internal/ent/metadata"
	"github.com/responserms/response/internal/ent/vehiclemodel"
)

// VehicleModel is the model entity for the VehicleModel schema.
type VehicleModel struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// CreatedBy holds the value of the "created_by" field.
	CreatedBy int `json:"created_by,omitempty"`
	// CreatedWith holds the value of the "created_with" field.
	CreatedWith string `json:"created_with,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// UpdatedBy holds the value of the "updated_by" field.
	UpdatedBy int `json:"updated_by,omitempty"`
	// UpdatedWith holds the value of the "updated_with" field.
	UpdatedWith string `json:"updated_with,omitempty"`
	// Short holds the value of the "short" field.
	Short string `json:"short,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VehicleModelQuery when eager-loading is set.
	Edges                  VehicleModelEdges `json:"edges"`
	vehicle_model_metadata *int
}

// VehicleModelEdges holds the relations/edges for other nodes in the graph.
type VehicleModelEdges struct {
	// Metadata holds the value of the metadata edge.
	Metadata *Metadata `json:"metadata,omitempty"`
	// Vehicles holds the value of the vehicles edge.
	Vehicles []*Vehicle `json:"vehicles,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// MetadataOrErr returns the Metadata value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VehicleModelEdges) MetadataOrErr() (*Metadata, error) {
	if e.loadedTypes[0] {
		if e.Metadata == nil {
			// The edge metadata was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: metadata.Label}
		}
		return e.Metadata, nil
	}
	return nil, &NotLoadedError{edge: "metadata"}
}

// VehiclesOrErr returns the Vehicles value or an error if the edge
// was not loaded in eager-loading.
func (e VehicleModelEdges) VehiclesOrErr() ([]*Vehicle, error) {
	if e.loadedTypes[1] {
		return e.Vehicles, nil
	}
	return nil, &NotLoadedError{edge: "vehicles"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VehicleModel) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case vehiclemodel.FieldID, vehiclemodel.FieldCreatedBy, vehiclemodel.FieldUpdatedBy:
			values[i] = new(sql.NullInt64)
		case vehiclemodel.FieldCreatedWith, vehiclemodel.FieldUpdatedWith, vehiclemodel.FieldShort, vehiclemodel.FieldTitle, vehiclemodel.FieldDescription:
			values[i] = new(sql.NullString)
		case vehiclemodel.FieldCreatedAt, vehiclemodel.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case vehiclemodel.ForeignKeys[0]: // vehicle_model_metadata
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type VehicleModel", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VehicleModel fields.
func (vm *VehicleModel) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case vehiclemodel.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			vm.ID = int(value.Int64)
		case vehiclemodel.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				vm.CreatedAt = value.Time
			}
		case vehiclemodel.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				vm.CreatedBy = int(value.Int64)
			}
		case vehiclemodel.FieldCreatedWith:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_with", values[i])
			} else if value.Valid {
				vm.CreatedWith = value.String
			}
		case vehiclemodel.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				vm.UpdatedAt = value.Time
			}
		case vehiclemodel.FieldUpdatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_by", values[i])
			} else if value.Valid {
				vm.UpdatedBy = int(value.Int64)
			}
		case vehiclemodel.FieldUpdatedWith:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field updated_with", values[i])
			} else if value.Valid {
				vm.UpdatedWith = value.String
			}
		case vehiclemodel.FieldShort:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field short", values[i])
			} else if value.Valid {
				vm.Short = value.String
			}
		case vehiclemodel.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				vm.Title = value.String
			}
		case vehiclemodel.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				vm.Description = value.String
			}
		case vehiclemodel.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field vehicle_model_metadata", value)
			} else if value.Valid {
				vm.vehicle_model_metadata = new(int)
				*vm.vehicle_model_metadata = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryMetadata queries the "metadata" edge of the VehicleModel entity.
func (vm *VehicleModel) QueryMetadata() *MetadataQuery {
	return (&VehicleModelClient{config: vm.config}).QueryMetadata(vm)
}

// QueryVehicles queries the "vehicles" edge of the VehicleModel entity.
func (vm *VehicleModel) QueryVehicles() *VehicleQuery {
	return (&VehicleModelClient{config: vm.config}).QueryVehicles(vm)
}

// Update returns a builder for updating this VehicleModel.
// Note that you need to call VehicleModel.Unwrap() before calling this method if this VehicleModel
// was returned from a transaction, and the transaction was committed or rolled back.
func (vm *VehicleModel) Update() *VehicleModelUpdateOne {
	return (&VehicleModelClient{config: vm.config}).UpdateOne(vm)
}

// Unwrap unwraps the VehicleModel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (vm *VehicleModel) Unwrap() *VehicleModel {
	tx, ok := vm.config.driver.(*txDriver)
	if !ok {
		panic("ent: VehicleModel is not a transactional entity")
	}
	vm.config.driver = tx.drv
	return vm
}

// String implements the fmt.Stringer.
func (vm *VehicleModel) String() string {
	var builder strings.Builder
	builder.WriteString("VehicleModel(")
	builder.WriteString(fmt.Sprintf("id=%v", vm.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(vm.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", created_by=")
	builder.WriteString(fmt.Sprintf("%v", vm.CreatedBy))
	builder.WriteString(", created_with=")
	builder.WriteString(vm.CreatedWith)
	builder.WriteString(", updated_at=")
	builder.WriteString(vm.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_by=")
	builder.WriteString(fmt.Sprintf("%v", vm.UpdatedBy))
	builder.WriteString(", updated_with=")
	builder.WriteString(vm.UpdatedWith)
	builder.WriteString(", short=")
	builder.WriteString(vm.Short)
	builder.WriteString(", title=")
	builder.WriteString(vm.Title)
	builder.WriteString(", description=")
	builder.WriteString(vm.Description)
	builder.WriteByte(')')
	return builder.String()
}

// VehicleModels is a parsable slice of VehicleModel.
type VehicleModels []*VehicleModel

func (vm VehicleModels) config(cfg config) {
	for _i := range vm {
		vm[_i].config = cfg
	}
}
